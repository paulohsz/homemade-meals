import { Button } from '@mui/material';

const ButtonHM = Button;
ButtonHM.defaultProps = {
  variant: 'contained',
};

// const Button2 = styled(Button)(() => ({}));
// Button2.defaultProps = {
//   variant: 'outlined',
// };

export default ButtonHM;
